/**
 * Shared Pact naming and paths for Story 1.5. Suite-local helper only;
 * not shared infrastructure. The generated contract is a version-controlled
 * source-of-truth artifact under specs/contracts/.
 */

export const CONSUMER_NAME = "qa-lab-api-consumer";
export const PROVIDER_NAME = "qa-lab-api";

export const PACT_FILE_NAME = `${CONSUMER_NAME}-${PROVIDER_NAME}.json`;
